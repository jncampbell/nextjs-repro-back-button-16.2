export default function DobPage() {
  return (
    <main>
      <h1>/dob</h1>
      <p>
        Press the browser <strong>Back</strong> button now.
      </p>
      <p>
        <strong>Bug:</strong> you land on the new-tab page (the entry before <code>/account</code>),
        not on <code>/account</code> itself.
      </p>
    </main>
  );
}
